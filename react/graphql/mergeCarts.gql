mutation mergeCarts(
  $savedCart: String!
  $currentCart: String!
  $strategy: Strategy
) {
  newOrderForm: mergeCarts(
    savedCart: $savedCart
    currentCart: $currentCart
    strategy: $strategy
  )
}
